---
import { Image } from 'astro:assets';
import AvatarPhoto from '../images/avatar.jpg';
import TappingPhoto from '../images/tapping.jpg';
import NeckPhoto from '../images/szyja.jpg';
import FacePhoto from '../images/twarz.jpg';
import FirstShouldersPhoto from '../images/barki.jpg';
import SecondShouldersPhoto from '../images/ramiona.jpg';
import HeroBackgroundImage from '../images/background.jpg';

type GridImage = { src: ImageMetadata; alt: string };

const gridImages: Array<GridImage> = [
	{ src: HeroBackgroundImage, alt: 'b' },
	{ src: TappingPhoto, alt: 'a' },
	{ src: NeckPhoto, alt: 'b' },
	{ src: FacePhoto, alt: 'a' },
	{ src: FirstShouldersPhoto, alt: 'b' },
	{ src: SecondShouldersPhoto, alt: 'b' },
];
---

<div class="mx-auto min-h-screen max-w-[1024px] scroll-mt-6 p-8" id="about">
	<div class="flex flex-col gap-4 md:h-64 md:flex-row md:gap-8">
		<Image
			src={AvatarPhoto}
			alt="To ja"
			class="h-auto w-auto object-cover sm:w-full md:w-64 lg:w-96"
		/>
		<div class="flex flex-col justify-between gap-2 md:gap-0">
			<h1 class="text-2xl font-semibold">Jestem Antonina</h1>
			<p class="text-justify text-base md:text-sm lg:text-base">
				Od 2008 roku z pasją i oddaniem prowadzę swój własny gabinet, który w
				ciągu tych lat stał się esencją mojego życiowego powołania. Moja ścieżka
				zawodowa przewinęła się przez najbardziej prestiżowe hotele spa w kraju,
				takie jak Malinowy Zdrój Hotel Medical Spa w Solcu Zdroju oraz Bristol
				Art & Medical Spa w Busko-Zdroju. Dla mnie, praca to nie tylko zawód,
				ale głęboko zakorzeniona pasja. Odkrywam w niej nieopisaną radość, mając
				szansę poprzez masaż i troskę o zdrowie przynosić ulgę i odprężenie moim
				pacjentom.
			</p>
		</div>
	</div>
	<div
		class="mt-8 hidden flex-wrap justify-center gap-8 md:flex md:justify-between"
	>
		{
			gridImages.map((image) => (
				<Image src={image.src} alt={image.alt} class="h-full w-auto sm:h-48" />
			))
		}
	</div>
</div>
